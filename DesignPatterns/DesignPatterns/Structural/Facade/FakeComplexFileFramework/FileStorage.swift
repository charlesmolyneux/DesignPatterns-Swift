public class FileStorage {
  public init() { }
  
  func save(data: Data, to destination: URL) throws {
    //Save Logic here
  }
  
  func load(fileId: String, from destination: URL) throws {
    //Load Logic here
  }
}
